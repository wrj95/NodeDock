services:
    container_name: nodeJS

    volumes: 
        - .:/app

    ports:
      
      - "8080:8080"

    command: bash -c "npm install; nodemon app.js --color=always"

    links: 
      - mongo

  mongo:
    image: mongo

    container_name: mgdb

    ports:
      - "27017:27017"
      